# Implementation Plans

Generated by the improve skill (focus: **visual layer + interactive tables**),
two passes against commit `3a29f98` (working tree also carried an uncommitted
Next 16.2.6 → 16.3.0 bump in `package.json`/`bun.lock`):

- **Pass 1 (2026-08-03)**: plans 001–004, under the repo's then-documented
  convention that charts are server-rendered, below-the-fold enhancements.
- **Pass 2 (2026-08-03, later same day)**: the maintainer **revoked that
  convention** — the explorer must be *visually rich*; interactive charts are
  first-class content ("for tables and lists users could just use the
  excels"). Plans 005–009 implement that direction; plan 003 is superseded.
  Still in force: provenance on every number, direct-costs-only framing,
  "No aplica" never rendered as $0, key figures also in server HTML, lazy
  chunks for performance.

Chart stack decision (pass 2): **shadcn charts / recharts now** (installed,
wrapper + theme tokens + 2 components exist); TanStack Charts is attractive
(~5× smaller) but its docs (v0.6.4, checked 2026-08-03) self-describe as
"pre-alpha … API may change between releases" with no SSR guidance — so it
gets a measured spike (009), not a bet. All chart components keep
library-agnostic props so a later swap is per-component.

**Reconciled 2026-08-04 against `d50cf63`**: the repo adopted Next 16.3
partial prefetching (params-under-Suspense App Shells, `next/link` in the big
tables, skeletons, a desglose error boundary) and BACKLOG now records a
decided architecture for `/buscar` (URL state + shallow routing). Plan 002
was rewritten to that decision (rev. 2); plans 004–008 got refreshed excerpts
and SHAs; plan 010 (TypeScript 7) was added at the maintainer's request.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Mapa de teselas en `/provincias` | P1 | S | — | DONE — approved 2026-08-04; branch `advisor/001-mapa-indice-provincias` (worktree `agent-adcf17d0888351399`), unmerged |
| 005  | Retirar la restricción «gráficos solo de servidor» (docs) | P1 | S | — | DONE — approved 2026-08-04; branch `advisor/005-direccion-visual` (worktree `agent-af55eb0fa44c35af5`), unmerged |
| 010  | TypeScript 7 (CLI checker nativo) | P1 | S | — | BLOCKED 2026-08-04 — typescript-eslint has no TS7 support (upstream #10940) and bun 1.3.3 resolves Microsoft's side-by-side alias circularly (hollow TS6 API, verified). Revisit triggers + an untested fix idea recorded in the plan header |
| 002  | `/buscar` con TanStack Table, estado en la URL (rev. 2) | P1 | M | — | DONE — approved 2026-08-04 after 2 revisions (native select + native input: 808 → 667 kB raw, 250 → 204 kB gz); branch `advisor/002-buscar-data-table` (worktree `agent-a2f38beb9ee5c678c`), unmerged. Ships the repo's only lint warning (`react-hooks/incompatible-library` on useReactTable, deliberate) |
| 006  | Curva de precios interactiva (140 provincias) en la página de ítem | P1 | M | 005 | DONE — approved 2026-08-04; branch `advisor/006-curva-precios-item` (worktree `agent-a774cae6e48326432`), unmerged. Approved deviation: `href` function prop → serializable `hrefBase` (functions can't cross the RSC boundary — plan spec bug). Human should hover/click/dark-check before merge |
| 007  | Visuales interactivos del hub de provincia (franja + capítulos) | P2 | M | 005 | DONE — approved 2026-08-04 after 1 revision (rank sentence hoisted to server HTML); branch `advisor/007-visuales-hub-provincia` (worktree `agent-a7cdbc47834d9f2da`), unmerged. Its build-time STOP was disproven by controlled A/B (37.4 s vs 36.5 s baseline) |
| 004  | Barras de participación por línea en el desglose | P2 | S | — | DONE — approved 2026-08-04; branch `advisor/004-barras-participacion-desglose` (worktree `agent-a9cde9fd2889a3ca2`), unmerged. Nit for later: right-align the bar track (`ml-auto`) |
| 008  | Treemap del costo en la página de desglose | P2 | M | 004 (soft) | DONE — approved 2026-08-04; branch `advisor/008-treemap-desglose` (worktree `agent-aa4b3a943f4827784`), unmerged. Tooltip + zero-cost guard verified in headless Chrome; light-mode label contrast still unreviewed by a human |
| 009  | Spike: evaluar TanStack Charts (informe, rama descartable) | P3 | S | 006 (soft) | DONE 2026-08-04 — verdict: **adopt at beta**. Chart chunk 100,7 → 22,6 KiB gz (~5,5×); full SSR works; React 19 declared. Report in the plan's Resultado section; spike branch `spike/009-tanstack-charts` unmerged by design |
| 003  | ~~Capa visual del hub (server-SVG)~~ | — | — | — | SUPERSEDED by 007 (direction change 2026-08-03) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale) | SUPERSEDED (pointer to successor)

## Dependency notes

- **005 first among the chart plans**: it updates the doc comments that
  currently contradict 006/007/008 (and edits the same item-page docblock 006
  touches). 001/002/004 are independent of the direction change.
- 006 vs 005: same file (`app/items/[codigo]/page.tsx`) — land 005 first.
- 008 after 004 (soft): both edit the desglose page; order avoids rebase
  churn only.
- 009 after 006 (soft): the spike ports 006's chart as its benchmark; without
  006 it falls back to `precio-bar.tsx`.
- 002 is the only plan adding a production dependency
  (`@tanstack/react-table`); 009 adds one on a throwaway branch only.

## Findings considered and rejected

- **TanStack Table on the existing static tables** (`/items`, hubs, the
  140-row provincia table): still rejected — this is about the *tables'* RSC
  payload and SEO role (`app/items/page.tsx:26-35`,
  `_components/tabla.tsx:1-14`), which the visual-richness direction does not
  revoke. Interactivity lives in `/buscar` (plan 002).
- **Adopting TanStack Charts outright** (pass 2): deferred to the 009 spike —
  pre-alpha per its own docs; the ~5× bundle win is real but unmeasured in
  this stack. Revisit with the spike's report.
- **Client-interactive tile map** (hover cards instead of native `<title>`
  tooltips on `ColombiaTileMap`): not now — the map's job (compare 33 known
  quantities, click through) is served; richness budget goes to the pages
  with zero or capped visuals first. Candidate for a later pass.
- **Home-page distribution chart** (histogram of the 140 medians): plausible
  under the new direction but unplanned — the home already has map + tiles +
  notables, and passes 006/007 cover the dispersion story where users
  actually compare. Plan on request.
- ~~Full 140-province visual on the item page: rejected~~ — **un-rejected by
  the direction change**; now plan 006.
- ~~Charts on the home page: fine as-is~~ — reframed above (unplanned, not
  rejected on principle).
